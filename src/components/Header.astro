<section id="inicio" class="relative bg-white min-h-screen flex items-center justify-center overflow-hidden">

	<!-- Efectos de fondo animados -->
	<div class="absolute inset-0">
		<!-- Patrón de grid sutil -->
		<div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
		
		<!-- Líneas decorativas horizontales -->
		<div class="absolute top-1/4 left-0 w-full h-px bg-gradient-to-r from-transparent via-orange-500/30 to-transparent"></div>
		<div class="absolute bottom-1/4 left-0 w-full h-px bg-gradient-to-r from-transparent via-gray-300/40 to-transparent"></div>
		<div class="absolute top-1/3 left-0 w-full h-px bg-gradient-to-r from-transparent via-orange-400/20 to-transparent animate-pulse"></div>
		<div class="absolute bottom-1/3 left-0 w-full h-px bg-gradient-to-r from-transparent via-gray-200/30 to-transparent animate-pulse"></div>
		
		<!-- Líneas decorativas verticales -->
		<div class="absolute top-0 left-1/4 w-px h-full bg-gradient-to-b from-transparent via-orange-500/20 to-transparent"></div>
		<div class="absolute top-0 right-1/4 w-px h-full bg-gradient-to-b from-transparent via-gray-300/25 to-transparent"></div>
		
		<!-- Líneas diagonales -->
		<div class="absolute top-0 left-0 w-full h-full">
			<div class="absolute top-1/4 left-1/4 w-64 h-px bg-gradient-to-r from-orange-500/30 to-transparent transform rotate-45 origin-left"></div>
			<div class="absolute bottom-1/4 right-1/4 w-48 h-px bg-gradient-to-l from-gray-300/30 to-transparent transform -rotate-45 origin-right"></div>
			<div class="absolute top-1/2 left-1/3 w-32 h-px bg-gradient-to-r from-orange-400/25 to-transparent transform rotate-12 origin-left animate-pulse"></div>
		</div>
	</div>

	<!-- Contenedor principal responsivo -->
	<div class="w-full px-4 sm:px-6 lg:w-4/5 lg:mx-auto lg:px-8 relative z-10">
		<div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-center min-h-screen py-12 sm:py-16 lg:py-20">
			<!-- Contenido de texto - Lado izquierdo -->
			<div class="lg:col-span-5 text-center lg:text-left space-y-6 sm:space-y-8 lg:pr-8">
				<!-- Título principal con efectos -->
				<div class="space-y-4">
					<h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-black leading-tight">
						<span class="block text-gray-900 animate-slide-up">
							TRANSFORMA
						</span>
						<span class="block text-transparent bg-clip-text bg-gradient-to-r from-orange-500 to-orange-600 animate-slide-up-delayed">
							TU CUERPO
						</span>
					</h1>
					<!-- Línea decorativa bajo el título -->
					<div class="w-32 h-1 bg-gradient-to-r from-orange-500 to-orange-400 animate-expand"></div>
				</div>
				
				<!-- Subtítulo -->
				<p class="text-lg sm:text-xl md:text-2xl text-gray-600 leading-relaxed max-w-xl animate-fade-in-up">
					Desbloquea tu <span class="text-orange-500 font-bold">máximo potencial</span> con nuestra línea premium de suplementos. 
					<span class="text-gray-900 font-semibold">Resultados visibles</span> desde el primer día.
				</p>
				
				<!-- Estadísticas -->
				<div class="grid grid-cols-2 gap-4 sm:gap-6 py-6 sm:py-8 animate-fade-in-up-delayed">
					<div class="text-center group">
						<div class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black text-orange-500 mb-1 sm:mb-2 group-hover:scale-110 transition-transform duration-300">+100</div>
						<div class="text-xs sm:text-sm text-gray-600 uppercase tracking-wider">Clientes Activos</div>
					</div>
					<div class="text-center group">
						<div class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-black text-gray-900 mb-1 sm:mb-2 group-hover:scale-110 transition-transform duration-300">99%</div>
						<div class="text-xs sm:text-sm text-gray-600 uppercase tracking-wider">Satisfacción</div>
					</div>
				</div>
				
				<!-- Botones de acción premium -->
				<div class="flex flex-col sm:flex-row gap-3 sm:gap-4 pt-4 animate-fade-in-up-delayed">
					<button class="group relative bg-gradient-to-r from-orange-500 to-orange-600 text-white px-6 sm:px-8 lg:px-10 py-3 sm:py-4 text-base sm:text-lg font-bold uppercase tracking-wider overflow-hidden transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-orange-500/25 rounded-lg cursor-pointer">
						<span class="relative z-10 flex items-center justify-center">
							Explorar Productos
							<svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
							</svg>
						</span>
						<div class="absolute inset-0 bg-gradient-to-r from-orange-600 to-orange-700 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left rounded-lg"></div>
					</button>
					
					<button class="group relative bg-transparent text-gray-900 px-6 sm:px-8 lg:px-10 py-3 sm:py-4 text-base sm:text-lg font-bold uppercase tracking-wider border-2 border-gray-300 overflow-hidden transition-all duration-300 hover:scale-105 hover:border-orange-500 hover:text-orange-500 rounded-lg cursor-pointer">
						<span class="relative z-10 group-hover:text-black transition-colors duration-300">Conoce Más</span>
						<div class="absolute inset-0 bg-white transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left rounded-lg"></div>
					</button>
				</div>
			</div>

			<!-- Imagen del producto - Lado derecho optimizado -->
			<div class="lg:col-span-7 relative flex justify-center lg:justify-end items-center min-h-[300px] sm:min-h-[400px] lg:min-h-[700px] mt-8 lg:mt-0">
				<!-- Contenedor con efectos mejorados -->
				<div class="relative w-full max-w-xs sm:max-w-sm md:max-w-lg lg:max-w-2xl xl:max-w-3xl">
					<!-- Círculo de fondo decorativo -->
					<div class="absolute inset-0 bg-gradient-to-br from-orange-50 to-orange-100 rounded-full scale-110 opacity-40 animate-pulse-slow"></div>
					
					<!-- Imagen principal con múltiples efectos -->
					<div class="relative z-10">
						<img 
							src="/prote-t.png" 
							alt="Proteína Premium PRIME SUPLEMENTOS" 
							class="w-full h-auto object-contain filter drop-shadow-xl animate-float-gentle hover:scale-110 transition-all duration-700 ease-out"
							style="max-height: 300px; min-height: 200px;"
						>
						
						<!-- Gradiente sutil sobre la imagen -->
						<div class="absolute inset-0 bg-gradient-to-t from-white/10 via-transparent to-transparent rounded-lg"></div>
						
						<!-- Sombra realista múltiple -->
						<div class="absolute -bottom-6 left-1/2 transform -translate-x-1/2 w-4/5 h-12 bg-gradient-to-r from-transparent via-gray-300/40 to-transparent rounded-full blur-xl"></div>
						<div class="absolute -bottom-3 left-1/2 transform -translate-x-1/2 w-3/5 h-6 bg-gray-400/20 rounded-full blur-md"></div>
					</div>
					
					<!-- Efectos decorativos alrededor -->
					<div class="absolute top-10 -left-4 w-20 h-20 bg-orange-200/30 rounded-full animate-bounce-slow"></div>
					<div class="absolute bottom-20 -right-6 w-16 h-16 bg-orange-300/20 rounded-full animate-bounce-slow" style="animation-delay: 1s;"></div>
					<div class="absolute top-1/3 -right-2 w-12 h-12 bg-orange-100/40 rounded-full animate-bounce-slow" style="animation-delay: 2s;"></div>
				</div>
			</div>
		</div>
	</div>
</header>

<style>
	.bg-grid-pattern {
		background-image: 
			linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
			linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
		background-size: 60px 60px;
	}

	.bg-gradient-radial {
		background: radial-gradient(circle, var(--tw-gradient-stops));
	}

	@keyframes float {
		0%, 100% { transform: translateY(0px) rotate(0deg); }
		50% { transform: translateY(-30px) rotate(180deg); }
	}

	@keyframes float-delayed {
		0%, 100% { transform: translateY(0px) rotate(0deg); }
		50% { transform: translateY(-40px) rotate(-180deg); }
	}

	@keyframes float-gentle {
		0%, 100% { transform: translateY(0px) scale(1); }
		50% { transform: translateY(-15px) scale(1.02); }
	}

	@keyframes slide-up {
		from { transform: translateY(100%); opacity: 0; }
		to { transform: translateY(0); opacity: 1; }
	}

	@keyframes slide-up-delayed {
		from { transform: translateY(100%); opacity: 0; }
		to { transform: translateY(0); opacity: 1; }
	}

	@keyframes fade-in {
		from { opacity: 0; transform: translateY(20px); }
		to { opacity: 1; transform: translateY(0); }
	}

	@keyframes fade-in-up {
		from { transform: translateY(30px); opacity: 0; }
		to { transform: translateY(0); opacity: 1; }
	}

	@keyframes fade-in-up-delayed {
		from { transform: translateY(30px); opacity: 0; }
		to { transform: translateY(0); opacity: 1; }
	}

	@keyframes expand {
		from { width: 0; }
		to { width: 8rem; }
	}

	@keyframes spin-slow {
		from { transform: rotate(0deg); }
		to { transform: rotate(360deg); }
	}

	@keyframes spin-reverse {
		from { transform: rotate(360deg); }
		to { transform: rotate(0deg); }
	}

	@keyframes bounce-slow {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-8px); }
	}

	@keyframes bounce-slow-delayed {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-8px); }
	}

	@keyframes pulse-slow {
		0%, 100% { opacity: 0.4; transform: scale(1.1); }
		50% { opacity: 0.6; transform: scale(1.15); }
	}

	.animate-float { animation: float 8s ease-in-out infinite; }
	.animate-float-delayed { animation: float-delayed 10s ease-in-out infinite; }
	.animate-float-gentle { animation: float-gentle 6s ease-in-out infinite; }
	.animate-slide-up { animation: slide-up 1s ease-out; }
	.animate-slide-up-delayed { animation: slide-up-delayed 1s ease-out 0.3s both; }
	.animate-fade-in { animation: fade-in 1s ease-out 0.5s both; }
	.animate-fade-in-up { animation: fade-in-up 1s ease-out 0.7s both; }
	.animate-fade-in-up-delayed { animation: fade-in-up-delayed 1s ease-out 0.9s both; }
	.animate-expand { animation: expand 1s ease-out 1.2s both; }
	.animate-spin-slow { animation: spin-slow 25s linear infinite; }
	.animate-spin-reverse { animation: spin-reverse 20s linear infinite; }
	.animate-bounce-slow { animation: bounce-slow 4s ease-in-out infinite; }
	.animate-bounce-slow-delayed { animation: bounce-slow-delayed 4s ease-in-out infinite 2s; }
	.animate-pulse-slow { animation: pulse-slow 8s ease-in-out infinite; }
</style>

<script>
	document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('#inicio button');
    
    if (buttons.length >= 2) {
        const explorarProductosBtn = buttons[0]; // "Explorar Productos"
        const conoceMasBtn = buttons[1]; // "Conoce Más"
        
        // Botón "Explorar Productos" → página de productos
        explorarProductosBtn.addEventListener('click', () => {
            window.location.href = '/productos';
        });
        
        // Botón "Conoce Más" → sección about us
        conoceMasBtn.addEventListener('click', () => {
            // Buscar sección about en la misma página
            const aboutSection = document.querySelector('#about');
            
            if (aboutSection) {
                // Si existe, hacer scroll
                aboutSection.scrollIntoView({ behavior: 'smooth' });
            } else {
                // Si no existe, ir a la página principal con anchor
                window.location.href = '/#about';
            }
        });
    }
});
</script>